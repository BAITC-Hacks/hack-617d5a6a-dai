# Приёмка решения «Граф денег»

Состояние на 23.09.2026: контрольные клиенты выбраны по `outputs/nodes_roles.csv` и `outputs/top_nodes.csv`. Числа ниже проверены по CSV после введения правила очереди. Запуск из чистого клона проверен. Проверка карточек в браузере пока не записана. После изменения расчёта нужно повторить выбор и сверку.

## Что проверяет аналитик

1. Ввести `gid` из таблицы ниже в поиск.
2. Проверить направление стрелок и даты переводов в окрестности клиента.
3. Сравнить роль, два основания приоритета и ограничения в карточке с таблицей ниже.
4. Прочитать следующий запрос и за минуту объяснить, какие данные нужны для проверки версии.

Роль описывает видимые переводы. `priority_raw` задаёт порядок проверки, а не вероятность нарушения. Статус «быстрый транзит» означает совпадение сумм и дат по правилу модели. Он не устанавливает, что через счёт прошли те же деньги.

## Пять контрольных клиентов

Выбор задан заранее: первый в рейтинге; следующий seed из первых 20; узел глубины 4 с наибольшим `priority_raw`; изолированный seed с наименьшим `gid`; первый в рейтинге оставшийся узел с флагом быстрого транзита и запросом по этому признаку.

| Случай | gid | Что показывают CSV | Проверка экрана |
| --- | --- | --- | --- |
| Первый в рейтинге | `100000003684369100` | Координатор, 41,2 балла, 24 наблюдаемых плательщика, 26 сопоставленных пар за 0–2 дня; входящие seed неполны | Не проверено |
| Другой seed из первых 20 | `100000003016635100` | Координатор, 33,3 балла, 73 наблюдаемых получателя; входящие seed неполны | Не проверено |
| Граница выгрузки | `100000003037476100` | Консолидатор, глубина 4, вход 555 тыс. KZT от трёх плательщиков; 6,8 балла, но вне очереди; исходящие не собирались | Не проверено |
| Изолированный seed | `100000000456947100` | Периферия, 0 баллов, переводов в выгрузке нет; это не означает отсутствия операций | Не проверено |
| Быстрый транзит | `100000003115284100` | Консолидатор, 26,7 балла, три сопоставленные пары за 0–2 дня; следующий запрос касается назначений платежей и остатков | Не проверено |

Для каждого случая после проверки в браузере записать: совпала ли карточка с CSV, понятна ли роль за минуту, что мешало понять вывод. Если какого-либо случая после пересчёта нет, записать это число и выбрать следующего клиента по тому же правилу.

На экране нужно отдельно проверить три вещи. Карточка должна показывать `in_queue`, поскольку 6,8 балла у пограничного клиента не включают его в очередь. Строка `next_request` должна быть видна без чтения CSV. Оценка `priority_score` в пределах 0..1 должна быть названа относительным приоритетом по этой выгрузке, чтобы число 1,0 не выглядело вероятностью нарушения. Код карточки и графа подключён к маршруту клиента; проверка в браузере остаётся открытой.

На скриншотах `docs/img/screen_node_neighborhood.jpg` и `docs/img/screen_overview.jpg` шириной 1280 px правая карточка не попала в кадр: страница требовала минимум 1440 px. Минимальная ширина страницы уменьшена до 1180 px, боковые панели стали уже на экранах меньше 1440 px. Нужен новый скриншот при 1280 px, где граф и карточка со следующим запросом видны одновременно. Поиск должен остаться доступным.

## Текст о приоритете для README и карточки

Приоритет помогает выбрать, кого проверить первым. Семь показателей описывают суммы переводов, число разных плательщиков и получателей, положение клиента между другими клиентами, меньшую из входящей и исходящей сумм и число исходных клиентов, связанных с ним по графу. Редкое значение даёт больше баллов. Повторные пары переводов похожей суммы за 0–2 дня добавляют один балл к сумме. Клиент попадает в очередь проверки, если хотя бы два из семи показателей входят в верхние 5 % среди 2 248 клиентов. Сумма баллов задаёт порядок внутри очереди и за её пределами. Значение 6 баллов служит справочным порогом суммы и не определяет очередь. Список `top_nodes.csv` содержит 50 клиентов независимо от очереди. Баллы не показывают вероятность нарушения. В карточке приведены два показателя с наибольшим вкладом.

`priority_score` в обязательном CSV лежит в пределах 0..1. Справочный порог 6 относится к `priority_raw`. Поле `in_queue` отмечает фактическое включение в очередь по двум показателям в верхних 5 %.

## Тексты следующего запроса

В `pipeline/evidence.py` есть шесть веток, включая отдельную ветку для изолированного seed. Строки ниже внесены в код. Каждая начинается с действия и короче 120 знаков.

| Ситуация | Текст |
| --- | --- |
| Глубина 4 | Запросить исходящие переводы клиента за июль 2026: на 4-м шаге их не собирали. |
| Seed | Запросить полные входящие переводы клиента, включая отправителей вне выборки. |
| Исходящий перевод датирован раньше сопоставленного входящего | Запросить остаток перед ранним исходящим переводом и поступления до него. |
| Быстрый транзит | Запросить назначения сопоставленных платежей и остатки на даты их проведения. |
| Изолированный seed | Запросить переводы клиента меньше 5 000 KZT и операции с другими банками за июль. |
| Остальные | Запросить выписку и сведения о контрагентах для крупнейших переводов клиента. |

Для роли `terminal` в карточке нужно показывать «получатель без видимых исходящих». Роль `coordinator` означает связь ветвей наблюдаемого графа. Она не устанавливает, кто управляет счетами.

## Проверка запуска и сдача

| Проверка | Результат |
| --- | --- |
| Обязательные CSV | 2 248 узлов, 50 строк в `top_nodes.csv`; `evidence` есть у каждого узла, максимум 191 знак; у узлов глубины 4 роль `terminal` не назначена |
| Чистый клон `f4de2b7` на Python 3.13.5 | `uv venv`, `uv pip install`, `make pipeline`: 28,6 с после загрузки библиотек; `make check`: 49 проверок, ошибок 0; CSV совпали с исходным коммитом |
| Пять карточек API после обновления текстов | Роль, `evidence`, `next_request` и `priority_score` совпали с CSV по всем пяти `gid` |
| Сборка интерфейса после подключения карточки и графа | `npm run build` завершилась без ошибок; визуальная проверка остаётся открытой |
| Экран через локальный прокси | После копирования `.env.example` с `VITE_MOCKS=false` запросы `/api/health`, `/api/meta` и карточки первого `gid` получили реальные ответы; `mock=false`, `source=outputs` |
| Поиск произвольного `gid` и пять карточек в браузере | Не проверено |
| Сдача на странице HackAlem | Не выполнено |
| Время сдачи и хеш отправленного коммита | Записать после сдачи |

## Ручной handoff, 23.09.2026 17:14

Проверка из пустого клона `/private/tmp/hackai-final-check.1h5F7m/repo`, коммит `7d0e75a`. На этой машине `python3` имеет версию 3.14.7, поэтому для А2–А5 использован описанный в README вариант с `uv` и Python 3.11.16. `make pipeline` завершился за 21,45 с, `make check` дал 49 проверок без ошибок. В CSV 2 248 узлов, 89 кластеров и 50 строк топа. В рабочем дереве клона после запуска изменился только `outputs/run_meta.json` с временем запуска.

| Пункт handoff | Результат |
| --- | --- |
| А1–А5 | Прошли по командам README с вариантом для Python 3.11; А3: 165 узлов в очереди |
| А6 | `/health` ответил `mock=false` |
| А7 | `make web` запустил Vite; главная страница отдала HTTP 200, прокси `/api/health` вернул `mock=false`, `/api/meta` указал `source=outputs`. Отрисовка в браузере не проверена |
| Б1–Б5 | Ответы API для пяти контрольных `gid` совпали с CSV по полям карточки. Отрисовка не проверена |
| Б6 | API открыл `gid` вне топа: `100000000011452100` |
| Б7 | API вернул 404 для `gid=123`; сообщение на экране не проверено |
| Б8 | Направление стрелок задано в коде графа; видимость на экране не проверена |
| Б9 | API отдаёт шесть русских названий ролей, включая «Получатель без видимых исходящих»; легенда на экране не проверена |
| Б10 | Устное объяснение карточки аналитиком не проверено |
| В1–В5 | README сверён с текущим кодом; технические упоминания `mock` оставлены в командах проверки API. 46 локальных ссылок проверены, отсутствующих файлов нет. Экранный текст не проверен |

Для завершения визуальной приёмки нужен новый скриншот при ширине 1280 px после исправления раскладки. На нём должны быть видны поиск и граф, а также карточка со следующим запросом. Сдачу через портал HackAlem и хеш отправленного коммита записать после фактической отправки.

## Срез перед сдачей, 23.09.2026 17:46

Проверенный коммит: `67d9d60`. Из пустого клона с Python 3.11.16 пересчитаны три CSV: они побайтно совпали с файлами в git. `make pipeline` и отдельный `make check` завершились с 49 успешными проверками; в очереди 165 узлов. После обновления клона до `67d9d60` проверка повторена, рабочее дерево чистое. В основном репозитории на этой версии прошли `npm run typecheck` и `npm run build`.

Скриншот `docs/img/coordinator.jpg` показывает граф, но не карточку. Поэтому проверка полного экрана при 1280 px, устное объяснение карточки и отправка в портале остаются непроверенными. Статус «сдано» и время отправки можно записать только после подтверждения на странице HackAlem.
