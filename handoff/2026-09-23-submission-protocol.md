# Протокол сдачи, 17:05–18:00

Владелец шага в скобках. Каждый шаг — «сделано / не сделано», без «почти».

| Время | Шаг | Кто | Как проверить |
|---|---|---|---|
| 17:05 | Краулер по всем эндпоинтам бэкенда для всех 2 248 узлов и 89 кластеров | Иван | отчёт: 0 сбоев, задержки p95 |
| 17:05–17:35 | Ручные проверки по `handoff/2026-09-23-amir-e2e-tests.md` на свежем клоне | Амир | таблицы заполнены, дефекты в чат сразу |
| 17:05–17:30 | Экран: `?? '/api'` в api.ts, стрелки-наконечники всегда, текст ошибки; `npm run typecheck && npm run build` | Дин | коммит и push до 17:30 |
| 17:30 | Заморозка всего кода; дальше только README и дефекты, найденные проверками | все | последний функциональный push у каждого |
| 17:30–17:40 | Дефекты из проверок Амира: чинит владелец участка, повторная проверка Амиром | Иван, Дин | |
| 17:40 | Финальный прогон: `make pipeline && make check`; `git status` — только ожидаемые изменения | Иван | 49/0, CSV совпадают с git |
| 17:42 | `git rm -r handoff/`; `grep -rn handoff README.md docs/ dai-front/README.md backend/README.md` — пусто | Иван | папки нет, ссылок нет |
| 17:44 | Секреты: `git grep -nE 'sk-|api_key|token' -- ':!*.lock' ':!package-lock.json'` — только ложные срабатывания; `.env*` не в git | Иван | |
| 17:46 | `git pull --rebase --autostash && git push`; `git log origin/main -1` = локальный HEAD | Иван, Дин, Амир | хеш совпал у всех троих |
| 17:48 | Свежий клон `origin/main` в пустую папку: `make setup && make pipeline && make check` | Иван | 49/0 |
| 17:50 | Хеш последнего коммита и время — в `docs/acceptance.md`; коммит и push | Амир | |
| 17:52 | Страница трека → «Сдать решение»: название «DAI — Граф денег», описание из README «Краткое описание» | Амир | статус «сдано» виден |
| 17:55 | Запас: если push не проходит — обход DNS уже в конфиге репозитория Ивана; у остальных — раздача с телефона | все | |

Если к 17:40 что-то из обязательного не работает: сдаём то, что в `origin/main` проходит `make check`, и записываем ограничение в README «Ограничения».
